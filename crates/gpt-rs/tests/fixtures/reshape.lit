EXPECTED:
program @captured (spec_version = ptir.v0.4) {
  func @captured {
    params:
      %0 : tensor<F32 x 2x3>
    results:
      %0 : tensor<F32 x 2x3>
  }
}
INPUT:
program @captured (spec_version = ptir.v0.4) {
  func @captured {
    params:
      %0 : tensor<F32 x 2x3>
    body:
      %1 = Reshape(ReshapeSpec { new_shape: [Explicit(Static(2)), Explicit(Static(3))] })(%0) -> tensor<F32 x 2x3>
    results:
      %1 : tensor<F32 x 2x3>
  }
}
run_test
